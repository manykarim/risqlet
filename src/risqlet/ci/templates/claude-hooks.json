{
  "_comment": "Merge this into your Claude Code settings.json (project .claude/settings.json or ~/.claude/settings.json). After each file write/edit, the risqlet CLI surfaces which tracked risks the change touches. Claude-Code-specific.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "risqlet check --files \"$CLAUDE_TOOL_FILE_PATH\" --json 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
